.header-logo__wrap
  = link_to'/', target: "" do
    = image_tag "logo.png", height:"30px"


  .wrapper
    .container
      %h1 Welcome
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        .form
          = link_to step1_signup_index_path, class: 'form input' do
            メールアドレスで登録
            = link_to'/', method: :post, class: 'btn-sns-facebook' do
              = fa_icon "facebook-square"
              Facebookで登録
            = link_to'/', method: :post, class: 'google' do
              =image_tag 'google.svg', alt: '', height: '45', width: '60',class: "google-logo"
              Googleで登録
          -# = f.text_field :nickname, autofocus: true, autocomplete: "nickname",class:"",placeholder: "Nickname"
          -# = f.email_field :email, autofocus: true, autocomplete: "email", class: "", placeholder:"E-mail"
          -# = f.password_field :password, autofocus: true, autocomplete: "password", class: "", placeholder:"Password"
          -# = f.submit "Sign up", class: "", id: "login-button"
      %ul.bg-bubbles
        %li
        %li
        %li
        %li
        %li
        %li
        %li
        %li
        %li
        %li
        %li
        %li








-# %h2 Sign up
-# = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
-#   = render "devise/shared/error_messages", resource: resource
-#   .field
-#     = f.label :email
-#     %br/
-#     = f.email_field :email, autofocus: true, autocomplete: "email"
-#   .field
-#     = f.label :password
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#     %br/
-#     = f.password_field :password, autocomplete: "new-password"
-#   .field
-#     = f.label :password_confirmation
-#     %br/
-#     = f.password_field :password_confirmation, autocomplete: "new-password"
-#   .actions
-#     = f.submit "Sign up"
-# = render "devise/shared/links"
